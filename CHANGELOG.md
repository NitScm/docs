# Changelog — the nit documentation

All notable changes to the user documentation site.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

A documentation site is continuously deployed rather than released, so this file
records **what changed and why a reader should care** — corrections especially.
A page that was wrong and is now right deserves an entry; a typo does not.

## [Unreleased]

Not yet deployed.

### The site

Astro 7 + Starlight, 25 pages, built from Markdown under `src/content/docs/`.
The sidebar is hand-written in `astro.config.mjs` rather than autogenerated,
because reading order matters: someone meeting nit for the first time has to
understand what a filtered projection is before anything else makes sense.

| | |
| --- | --- |
| `start/` | What nit does, install, a first workspace, a first policy |
| `concepts/` | Filtered projections, authorization, guards, push and pull, architecture, glossary |
| `guides/` | Policy bundles, server, workers, forges, console, operations, production, and a worked GitHub-over-SSH example |
| `reference/` | `nit`, `nitctl`, configuration settings, the HTTP API, error codes |

`dev.sh` selects Node from `.nvmrc` through nvm, installs it when missing, and
runs `npm install` when the lockfile is newer than `node_modules`.

### Corrections worth recording

Each of these was wrong in the direction of making things look safer or simpler
than they were, which is the direction that costs a reader most.

- **Audit retention.** Pages stated that retention was "handled with partitions
  and a privileged role". `audit_log` is not partitioned and nothing prunes it —
  and because the table is append-only at the database level, a plain `DELETE`
  reports success and removes nothing. The pages now say so and give the purge
  procedure that actually works.
- **systemd credentials.** An example used
  `sync_key_file: ${CREDENTIALS_DIRECTORY}/sync-key`. nit does not expand
  variables inside configuration values, so that path would have been read
  literally and failed. Replaced with the environment-layer form, where systemd
  expands `%d` itself.
- **Clone counts in the worked example.** The page claimed 11 files delivered
  and 3 withheld; the real figures against that bundle are 7 and 5, because
  `.gitattributes` and `.github/workflows/ci.yml` are covered by no read rule at
  all. The page now uses that as the illustration of closed-by-default.
- **A denial that named no rule.** The example showed a refusal attributed to a
  rule where the engine actually reported `no_matching_rule`. Rather than fake
  the output, the example bundle gained an explicit deny rule with a
  description — which is the practice the page should have been teaching anyway.
- **Guard identifier.** `protected-path` corrected to `protected_path`.

### Deployment

Published to GitHub Pages at **<https://nitscm.github.io/docs/>** from
`.github/workflows/deploy.yml`: built on every pull request, deployed when
`main` moves. No custom domain and no DNS — GitHub provides the certificate.

Served as a *project page*, so everything lives under `base: '/docs'`. That
has one consequence worth knowing before writing a page: Starlight prefixes the
links it generates itself — sidebar, pagination, breadcrumbs — but a link an
author types is emitted verbatim, and would 404. Measured before it was fixed:
28 broken links against 23 working ones, with no error at build time.

A rehype plugin in `astro.config.mjs` applies the base to every root-absolute
link in Markdown, so pages keep being written as `[x](/concepts/guards/)` and
the base stays declared in one place. Component props and YAML frontmatter sit
outside that pipeline, which is why `index.mdx` uses
`import.meta.env.BASE_URL` for its cards and relative paths for its hero
actions.

### Known gaps

- Link checking is manual; the build catches a sidebar slug with no page, but
  not a link to a page that does not exist.
- No search beyond Starlight's built-in Pagefind index.
- No versioned documentation — the site describes `main`.
- No translations.

---

## Release notes will start here

```
## [YYYY-MM-DD]

### Added
### Changed
### Corrected
```

**Corrected** is a section of its own, on purpose. A reader who followed a page
that was wrong needs to find out; burying it among "Changed" is how they do not.
